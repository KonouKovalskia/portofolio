// Tailwind CSS v4 uses CSS-first configuration.
// Theme tokens (colors, fonts, etc.) are defined in src/app/globals.css via @theme.
// This file is intentionally minimal — do not add v3-style theme extensions here.

/** @type {import('tailwindcss').Config} */
const config = {
  content: ['./src/**/*.{js,jsx,ts,tsx}'],
}

export default config